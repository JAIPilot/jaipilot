interface:
  display_name: "Clean & Prove Java Changes"
  short_description: "OpenRewrite-first cleanup with build proof"
  default_prompt: "Use $jaipilot-clean-java to clean the requested Java code and apply only a build-verified, drift-safe candidate."
