interface:
  display_name: "Accelerate Java Verification"
  short_description: "Run long Java checks with safe parallelism"
  default_prompt: "Use $jaipilot-fast-execution to run this substantial Java verification as quickly as safely possible, defaulting execution to JAIPilot Remote unless laptop-only resources are required."
