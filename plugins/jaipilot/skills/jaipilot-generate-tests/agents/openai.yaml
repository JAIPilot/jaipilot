interface:
  display_name: "Maximize Java Test Coverage"
  short_description: "Raise meaningful per-class Java coverage"
  default_prompt: "Use $jaipilot-generate-tests to raise meaningful class-level Java test coverage toward at least 80% with bounded parallel workers and fresh repository evidence."
