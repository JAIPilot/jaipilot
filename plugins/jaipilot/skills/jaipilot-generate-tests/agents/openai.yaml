interface:
  display_name: "Generate Java Tests"
  short_description: "Add behavior-focused Java tests"
  default_prompt: "Use $jaipilot-generate-tests to add meaningful Java tests and verify them with the repository's existing tools."
