interface:
  display_name: "Run Java on Remote Hardware"
  short_description: "Run long Java checks on ready hardware"
  default_prompt: "Use $jaipilot-remote-java to run this substantial Java build or benchmark on authenticated disposable hardware and report the exact evidence and benefit."

dependencies:
  tools:
    - type: "mcp"
      value: "jaipilot-remote"
      description: "Authenticated disposable Java execution"
      transport: "streamable_http"
      url: "https://api.jaipilot.com/functions/v1/jaipilot-cloud/mcp"
