interface:
  display_name: "Run Java on Remote Hardware"
  short_description: "Offload long checks for an exact commit"
  default_prompt: "Use $jaipilot-remote-java to run this committed Java build or benchmark on disposable remote hardware and return exact evidence."
